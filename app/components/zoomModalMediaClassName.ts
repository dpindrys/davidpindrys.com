/**
 * Lightbox / zoom-modal media: intrinsic width and height, never upscaled;
 * `max-w-full` caps to the modal container; `max-h-[92vh]` prevents vertical overflow.
 */
export const ZOOM_MODAL_MEDIA_CLASSNAME =
  "min-w-0 max-h-[92vh] w-auto max-w-full border-0 object-contain shadow-none";
